apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: retail-rs
  namespace: likhitha-ns
  labels:
    app: retail
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
        - name: retail-container
          image: likhithabhogyam/retail-inventory-app:latest
          ports:
            - containerPort: 3000
 